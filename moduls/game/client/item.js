//Template.requirements.helpers({
	/*requirements: function() {
	var item = Session.get('active_item');

	var requirements = [];
	for (var key in item.requirements) {
		requirements.push({
			engName: item.requirements[key][0].engName,
			name: item.requirements[key][0].name,
			type: item.requirements[key][0].type,
			url: item.requirements[key][0].url,
			level: item.requirements[key][1]
		})
	}

	return requirements;
	
	//Session.set('requirements', requirements);
}*/
//})